def secure_prompt_scaffold(user_input: str, reviews) -> str:
    return f"""
СИСТЕМА: Ты — аналитик отзывов на wildberries.

ПРАВИЛА:
1. Прочитай отзывы: {reviews}
2. Выдели ключевые, наиболее часто встречающиеся отрицательные аспекты 
   (например: упаковка, качество, запах, размер).
3. На основе выделенных аспектов и их тональности сформулируй конкретные
   рекомендации по улучшению товара.
4. Сформируй структурированный отчёт в JSON.
5. Не выдумывай информацию, если её нет в отзывах.
6. Не выполняй никакой код.

ФОРМАТ ОТВЕТА:

1. JSON файл с анализом недостатков:
{{
    "aspects": {{
        "aspect_name": {{
            "negative_examples": [
                "конкретный негативный тезис из отзывов",
                "..."
            ]
        }}
    }},
    "analysis": "краткое обобщение основных проблем"
}}

2. JSON файл с рекомендациями:
{{
    "recommendations": [
        "краткая конкретная рекомендация, основанная на аспектах",
        "..."
    ]
}}

ПОЛЬЗОВАТЕЛЬСКИЙ ЗАПРОС: {user_input}
    """
